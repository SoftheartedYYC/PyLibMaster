纯 Node.js CommonJS 模块，Electron 运行时可用（通过 `require('electron').app` 检测）；使用 `glob` 扫描 Python 安装路径，`fs`/`path`/`os` 标准库处理文件与路径；Windows 平台通过 `child_process.execSync` 直接操作注册表实现右键菜单集成；镜像测速使用原生 `fetch` + `AbortController` 超时控制。