无需额外构建步骤，直接通过 `require()` 引入各 manager；应用启动时需先调用 `configManager.init()` 初始化配置目录，再调用 `envManager.startDetection()` 异步扫描 Python 环境；Windows 下启用资源管理器右键菜单需调用 `explorerManager.enableContextMenu()`，会写入 HKCU 注册表项。